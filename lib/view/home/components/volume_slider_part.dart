import 'package:flutter/material.dart';

class VolumeSliderPart extends StatelessWidget {
  const VolumeSliderPart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("VolumeSliderPart"),
      ),
    );
  }
}
