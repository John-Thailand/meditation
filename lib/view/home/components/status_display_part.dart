import 'package:flutter/material.dart';

class StatusDisplayPart extends StatelessWidget {
  const StatusDisplayPart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("StatusDisplayPart"),
      ),
    );
  }
}
